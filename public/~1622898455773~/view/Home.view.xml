<mvc:View 
    controllerName="com.perezjquim.ytc.pwa.controller.Home" 
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.ui.layout.form"
    xmlns:mvc="sap.ui.core.mvc">
    <Page 
    	title="{i18n>Home}">
        <f:SimpleForm 
        	editable="false">
            <f:content>
                <Label text="{i18n>url}"/>
                <Input value="{prompt>/url}"/>
                <Label text="{i18n>start_time}"/>
                <Input value="{prompt>/start_time}"/>
                <Label text="{i18n>duration}"/>
                <Input value="{prompt>/duration}"/>
                <Label value="{i18n>download}"/>
                <HBox justifyContent="SpaceBetween">                    
                    <Button 
                    	icon="sap-icon://download" 
                    	press="onPressDownload"
                    	type="Emphasized" />
                </HBox>
            </f:content>
        </f:SimpleForm>           
    </Page>
</mvc:View>